# =============================================================================
# LegalIA API - base runtime dependencies
# =============================================================================
# Local embedding models (BGE-M3) are NOT installed here on purpose: torch adds
# ~2.5 GB to the image and the 8 GB VPS is not expected to serve BGE-M3 inline.
# See requirements-local-embeddings.txt for that optional extra.

# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12

# --- Validation / settings ---
pydantic==2.9.2
pydantic-settings==2.5.2
# Backs pydantic's EmailStr; not a transitive dependency of pydantic itself.
email-validator==2.2.0

# --- Database ---
sqlalchemy==2.0.35
alembic==1.13.3
psycopg[binary]==3.2.3
pgvector==0.3.6

# --- Auth ---
# bcrypt is used directly: passlib 1.7.4 reads bcrypt.__about__.__version__,
# which bcrypt >= 4.1 removed, and emits a spurious error on every hash.
pyjwt==2.9.0
bcrypt==4.2.0

# --- HTTP / providers ---
httpx==0.27.2
anthropic==0.39.0
tenacity==9.0.0

# --- Ingestion & Document Parsing (MarkItDown) ---
pypdf==5.0.1
python-docx==1.2.0
markitdown==0.1.7
beautifulsoup4==4.12.3
lxml==5.3.0

# --- Tokenization / math ---
tiktoken==0.8.0
numpy==1.26.4

# --- Config & Search ---
python-dotenv==1.0.1
ddgs>=9.16.0
primp>=2.0.0
